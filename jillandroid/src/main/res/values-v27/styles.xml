<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Renamed from GotTheme during this project's pre-release cleanup: see values/styles.xml's
         own comment for why "descore" fits better than either game's own name here. -->
    <style name="DescoreFullscreenTheme" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <item name="android:windowLayoutInDisplayCutoutMode">
            shortEdges
        </item>
    </style>
</resources>
