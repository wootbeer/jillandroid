<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Renamed from GotTheme during this project's pre-release cleanup: plain immersive
         fullscreen theme, no GoT-specific content at all, shared by both of wootbeer.jillandroid's
         own Activities, JillActivity and JillGameActivity (see AndroidManifest.xml), so
         "descore" fits better than either game's own name. -->
    <style name="DescoreFullscreenTheme" parent="@android:style/Theme.NoTitleBar.Fullscreen" />
</resources>
